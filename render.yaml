services:
  - type: web
    name: botteu
    runtime: python
    region: frankfurt          # same region as the DB
    plan: free                 # upgrade to starter for always-on
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -c gunicorn.conf.py run:app
    healthCheckPath: /
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true    # Render generates a random value on first deploy
      - key: DATABASE_URL
        sync: false            # set in Render dashboard → Environment
      - key: DB_SCHEMA
        value: botteu
      - key: FERNET_KEY
        sync: false            # set in Render dashboard → Environment
      - key: TELEGRAM_BOT_TOKEN
        sync: false            # set in Render dashboard → Environment
      - key: MAIL_SERVER
        value: smtp.gmail.com
      - key: MAIL_PORT
        value: "587"
      - key: MAIL_USE_TLS
        value: "True"
      - key: MAIL_USERNAME
        sync: false
      - key: MAIL_PASSWORD
        sync: false
      - key: MAIL_DEFAULT_SENDER
        value: "BOTTEU <andrii.it.info@gmail.com>"
      - key: APP_URL
        value: https://botteu.onrender.com
      - key: TELEGRAM_WEBHOOK_URL
        value: https://botteu.onrender.com/telegram/webhook
      - key: STRIPE_PUBLIC_KEY
        sync: false
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: STRIPE_PRICE_ID_PRO
        sync: false
